apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-inventory
    newName: ${ECR_REGISTRY}/homebase-inventory
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
