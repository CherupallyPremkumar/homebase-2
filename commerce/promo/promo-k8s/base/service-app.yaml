apiVersion: v1
kind: Service
metadata:
  name: promo
  namespace: promo
  labels:
    app.kubernetes.io/name: promo
    app.kubernetes.io/part-of: homebase
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: promo
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
