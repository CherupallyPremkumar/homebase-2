apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-checkout
    newName: ${ECR_REGISTRY}/homebase-checkout
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
