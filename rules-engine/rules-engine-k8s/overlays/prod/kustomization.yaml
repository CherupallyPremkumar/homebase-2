apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-rules-engine
    newName: ${ECR_REGISTRY}/homebase-rules-engine
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
