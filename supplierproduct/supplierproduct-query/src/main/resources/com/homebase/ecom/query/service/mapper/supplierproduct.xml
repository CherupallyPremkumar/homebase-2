<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Supplierproduct">

<resultMap id="result" type="map">
   <result property="id" column="id"/>
   <result property="supplierId" column="supplier_id"/>
   <result property="productId" column="product_id"/>
   <result property="status" column="status"/>
   <result property="stateId" column="stateId"/>
   <result property="createdAt" column="created_at"/>
</resultMap>

<select id='getAll-count' resultType="int">
	select count(*) from supplierproduct
    <where>
        <if test="supplierId != null">
            supplier_id = #{supplierId}
        </if>
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
</select>

<select id="getAll" resultMap="result">	
    SELECT id, supplier_id, product_id, status, "stateId", created_at FROM supplierproduct
    <where>
        <if test="supplierId != null">
            supplier_id = #{supplierId}
        </if>
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
    ${orderby} ${pagination}
</select>

</mapper>