apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-order
    newName: ${ECR_REGISTRY}/homebase-order
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
