apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: order
  labels:
    app.kubernetes.io/name: order
    app.kubernetes.io/part-of: homebase
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
