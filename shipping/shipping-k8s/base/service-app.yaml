apiVersion: v1
kind: Service
metadata:
  name: shipping
  namespace: shipping
  labels:
    app.kubernetes.io/name: shipping
    app.kubernetes.io/part-of: homebase
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shipping
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
