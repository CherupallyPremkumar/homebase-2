<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Shipping">

<resultMap id="result" type="map">
   <result property="id" column="id"/>
   <result property="orderId" column="order_id"/>
   <result property="carrier" column="carrier"/>
   <result property="trackingNumber" column="tracking_number"/>
   <result property="status" column="status"/>
   <result property="stateId" column="stateId"/>
   <result property="createdAt" column="created_at"/>
</resultMap>

<select id='getAll-count' resultType="int">
	select count(*) from shipments
    <where>
        <if test="orderId != null">
            order_id = #{orderId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
</select>

<select id="getAll" resultMap="result">	
    SELECT id, order_id, carrier, tracking_number, status, "stateId", created_at FROM shipments
    <where>
        <if test="orderId != null">
            order_id = #{orderId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
    ${orderby} ${pagination}
</select>

</mapper>