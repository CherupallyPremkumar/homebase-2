<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.homebase.ecom.demoorder</groupId>
        <artifactId>demo-order-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>demo-order-app</artifactId>
    <packaging>jar</packaging>
    <name>demo-order-app</name>
    <description>Standalone Spring Boot application for demo-order (runs on port 8080)</description>

    <dependencies>
        <!-- Demo-order modules -->
        <dependency>
            <groupId>com.homebase.ecom.demoorder</groupId>
            <artifactId>demo-order-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.homebase.ecom.demoorder</groupId>
            <artifactId>demo-order-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>com.homebase.ecom.demoorder</groupId>
            <artifactId>demo-order-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.homebase.ecom.demoorder</groupId>
            <artifactId>demo-order-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.homebase.ecom.demoorder</groupId>
            <artifactId>demo-order-kafka</artifactId>
        </dependency>

        <!-- Spring Boot and Chenile Infrastructure -->
        <dependency>
            <groupId>org.chenile</groupId>
            <artifactId>chenile-http</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Security API (for no-op SecurityService bean) -->
        <dependency>
            <groupId>org.chenile</groupId>
            <artifactId>chenile-security-api</artifactId>
        </dependency>

        <!-- Service Registry (Chenile ProxyUtils needs this) -->
        <dependency>
            <groupId>org.chenile</groupId>
            <artifactId>service-registry-service</artifactId>
        </dependency>
        <dependency>
            <groupId>org.chenile</groupId>
            <artifactId>service-registry-delegate</artifactId>
        </dependency>

        <!-- Kafka -->
        <dependency>
            <groupId>org.chenile</groupId>
            <artifactId>chenile-kafka</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.homebase.ecom.demoorder.DemoOrderApp</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
