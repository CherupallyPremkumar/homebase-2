apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-settlement
    newName: ${ECR_REGISTRY}/homebase-settlement
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
