apiVersion: v1
kind: Service
metadata:
  name: settlement
  namespace: settlement
  labels:
    app.kubernetes.io/name: settlement
    app.kubernetes.io/part-of: homebase
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: settlement
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
