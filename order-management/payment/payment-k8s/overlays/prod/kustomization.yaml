apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-payment
    newName: ${ECR_REGISTRY}/homebase-payment
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
