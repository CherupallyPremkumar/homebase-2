apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homebase-infra

resources:
  - namespace.yaml
  - kafka-deployment.yaml
  - kafka-service.yaml
  - kafka-topic-creator-job.yaml
