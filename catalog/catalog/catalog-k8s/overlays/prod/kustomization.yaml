apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-catalog
    newName: ${ECR_REGISTRY}/homebase-catalog
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
