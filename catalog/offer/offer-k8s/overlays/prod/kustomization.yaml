apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-offer
    newName: ${ECR_REGISTRY}/homebase-offer
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
