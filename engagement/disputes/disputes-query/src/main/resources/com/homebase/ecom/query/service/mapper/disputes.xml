<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Disputes">
  <select id="getAll" resultType="map">SELECT id, order_id AS "orderId", customer_id AS "customerId", reason, status, priority, resolution, created_time AS "createdAt" FROM disputes <where><if test="orderId != null">AND order_id = #{orderId}</if><if test="status != null">AND status = #{status}</if><if test="priority != null">AND priority = #{priority}</if></where> ORDER BY created_time DESC</select>
  <select id="getAll-count" resultType="long">SELECT COUNT(*) FROM disputes <where><if test="status != null">AND status = #{status}</if><if test="priority != null">AND priority = #{priority}</if></where></select>
</mapper>
