package com.homebase.ecom.support.dto;

import org.chenile.workflow.param.MinimalPayload;

/**
 * Payload for starting work on a ticket (ASSIGNED -> IN_PROGRESS).
 */
public class StartWorkPayload extends MinimalPayload {
}
