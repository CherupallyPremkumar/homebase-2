apiVersion: v1
kind: Service
metadata:
  name: pricing
  namespace: pricing
  labels:
    app.kubernetes.io/name: pricing
    app.kubernetes.io/part-of: homebase
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pricing
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
