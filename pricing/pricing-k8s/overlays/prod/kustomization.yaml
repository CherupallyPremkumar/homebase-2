apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-pricing
    newName: ${ECR_REGISTRY}/homebase-pricing
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
