package com.homebase.ecom.support.dto;

import org.chenile.workflow.param.MinimalPayload;

/**
 * Payload for closing a support ticket (RESOLVED -> CLOSED).
 */
public class CloseTicketPayload extends MinimalPayload {
}
