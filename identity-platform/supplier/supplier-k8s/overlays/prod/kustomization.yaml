apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-supplier
    newName: ${ECR_REGISTRY}/homebase-supplier
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
