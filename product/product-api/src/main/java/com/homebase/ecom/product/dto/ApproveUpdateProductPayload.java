package com.homebase.ecom.product.dto;

import org.chenile.workflow.param.MinimalPayload;

/**
 * Payload for the approveUpdate event.
 * Admin approves the pending changes — they go live on the product.
 */
public class ApproveUpdateProductPayload extends MinimalPayload {
}
