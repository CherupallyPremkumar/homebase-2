<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Product">

<select id='getAll-count' resultType="int">
	select count(*) from products
    <where>
        <if test="name != null">
            name like #{name}
        </if>
        <if test="category != null">
            AND category = #{category}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
</select>

<select id="getAll" resultType="com.homebase.ecom.dto.ProductDto">	
    SELECT 
        id, 
        name, 
        description, 
        category, 
        image_url as "imageUrl", 
        status, 
        "stateId" 
    FROM products
    <where>
        <if test="name != null">
            name like #{name}
        </if>
        <if test="category != null">
            AND category = #{category}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
    ${orderby} ${pagination}
</select>

<select id="getById" resultType="com.homebase.ecom.dto.ProductDto">
    SELECT 
        id, 
        name, 
        description, 
        category, 
        image_url as "imageUrl", 
        status, 
        "stateId" 
    FROM products
    WHERE id = #{id}
</select>

</mapper>