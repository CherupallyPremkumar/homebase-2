<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Supplier">

<resultMap id="result" type="map">
   <result property="id" column="id"/>
   <result property="name" column="name"/>
   <result property="description" column="description"/>
   <result property="email" column="email"/>
   <result property="status" column="status"/>
   <result property="stateId" column="stateId"/>
   <result property="createdAt" column="created_at"/>
</resultMap>

<select id='getAll-count' resultType="int">
	select count(*) from supplier
    <where>
        <if test="name != null">
            name like #{name}
        </if>
        <if test="email != null">
            AND email like #{email}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
</select>

<select id="getAll" resultMap="result">	
    SELECT id, name, description, email, status, "stateId", created_at FROM supplier
    <where>
        <if test="name != null">
            name like #{name}
        </if>
        <if test="email != null">
            AND email like #{email}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="stateId != null">
            AND "stateId" = #{stateId}
        </if>
    </where>
    ${orderby} ${pagination}
</select>

</mapper>