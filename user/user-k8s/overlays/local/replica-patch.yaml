apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: user
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: app
          image: homebase/user-app:latest
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "local"
