apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - hpa.yaml

images:
  - name: homebase-promo
    newName: ${ECR_REGISTRY}/homebase-promo
    newTag: latest

patchesStrategicMerge:
  - replica-patch.yaml
