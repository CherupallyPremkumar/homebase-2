package com.homebase.ecom.notification.dto;

import org.chenile.workflow.param.MinimalPayload;

/**
 * Payload for the retry event (FAILED -> CHECK_RETRY auto-state).
 */
public class RetryNotificationPayload extends MinimalPayload {
}
